# the yml file takes precedence over .properties
server:
  port: 8080
 
spring:
  datasource:
#    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:root}
    url: jdbc:mysql://sam-my-sql.cmtthcn4z2p3.us-east-2.rds.amazonaws.com:3306/sam-my-sql
#    username: ${MYSQL_USERNAME:root}
    username: sam-my-sql
#    password: ${MYSQL_PASSWORD:root}
    password: sam-my-sql
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect